#include "header.h"

/*t_point **alloc_trajet(int n)
{
    int i;
    t_point **trajet=NULL;
    trajet=(t_point**)malloc(n*sizeof(t_point*));
    for (i=0;i<n;i++)
    {
        trajet[i]=(t_point*)malloc(n*sizeof(t_point));
    }

    return trajet;
}

t_point *alloc_point()
{
    t_point *point=NULL;
    point=(t_point*)malloc(1*sizeof(t_point));

    return point;
}*/

